<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.NewsApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color - Softer Purple -->
        <item name="colorPrimary">@color/primary_purple_soft</item>
        <item name="colorPrimaryVariant">@color/primary_purple_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background and Surface - Adjusted for better contrast in light theme -->
        <item name="android:windowBackground">@color/light_bg_main</item>
        <item name="colorSurface">@color/light_bg_article_card</item>
        <item name="colorOnSurface">@color/text_primary_light</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/primary_purple_dark</item>

        <!-- Global Text Colors -->
        <item name="android:textColorPrimary">@color/text_primary_light</item>
        <item name="android:textColorSecondary">@color/text_grey_strong</item>

        <!-- Action Bar theme -->
        <item name="actionBarTheme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
    </style>
</resources>